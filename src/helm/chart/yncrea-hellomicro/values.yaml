# Default values for yncrea-hellomicro.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: test/cloudcomputing
  version: latest

service:
  type: NodePort
  nodePort: 30080